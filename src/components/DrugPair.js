import React from "react";

const DrugPair = ({ description }) => {
  // console.log(`🪲${JSON.stringify(JSON.stringify(props))}`);
  return <li>{description}</li>;
  // return <div>Hello</div>;
};

export default DrugPair;
